# 🖼️ Image Captioning (CNN Encoder + LSTM Decoder)

## 📚 Overview
This project implements an image captioning model that generates natural language captions for images using:
- **CNN (ResNet-50)** as an Encoder to extract image features.
- **LSTM** as a Decoder to generate captions.

---

## ⚙️ Project Structure
